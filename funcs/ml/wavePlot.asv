function wavePlot(trackName,path,sf)
    trackName = string(trackName);
    path = string(path);
    
    if ~ endsWith(trackName,string('.csv'))
        trackName = trackname+''
    end

    filePath = (string(path)+string(trackName))
    
%     track = csvread(path+trackName);
end

